export enum CustomerFormFieldsEnum {
    STATUS = "status",
    THREE_WORDS_UBICATION = "threeWordsUbication",
}

export enum CustomerFormContactEnum {
    NAME = "contact.name",
    LAST_NAME = "contact.lastName",
    PHONE_NUMBER = "contact.phoneNumber",
    ADRESS = "contact.address"
}